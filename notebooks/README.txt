sensor_anomaly/notebooks/ — .ipynb 만 둡니다. 빌드 스크립트는 ../scripts/

clean/        로컬 전체 파이프라인 + EDA 시각화 (vif_clean, all_clean)
kaggle/       clean에서 시각화 축소한 Kaggle 제출용 (scripts/build_kaggle_noviz.py 로 생성)
val_compare/  Sec.6 default vs tuned Val 지표 표 (scripts/build_val_compare_nb.py 로 생성)
legacy/       예전 대용량 노트북 (참고)
